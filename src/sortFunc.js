function sortFunc(obj,str) {
    let arr = str.split(".")
    for (let key of arr ){
        if(obj[key] && typeof(obj[key]) === "object"){
            
        } else {
            
            
        }
    }
}

(obj,str)  => {
    let arr = str.split(".")
    for (let key of arr ){
        if(obj[key] && typeof(obj[key]) === "object"){
            return obj
        } else {
            return null
        }
    }
}

let ex  = {posts:
st.promiseReducer &&
st.promiseReducer.UserPosts &&
st.promiseReducer.UserPosts.payload &&
st.promiseReducer.UserPosts.payload.PostFind}
console.log(777777);